use super::*;
use std::{ops::Add, simd::*};

#[cfg(test)]
mod tests {
    #[test]
    fn f() {
        use super::*;
        use crate::Edge::*;
        // let U = Edges::swap(UF, UR) + Edges::swap();
        println!("hai");
    }
}
